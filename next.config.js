/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  assetPrefix: "/live/", //加前缀
  basePath: "/live", //node
  //distDir: "dist/pc",
};

module.exports = nextConfig;
